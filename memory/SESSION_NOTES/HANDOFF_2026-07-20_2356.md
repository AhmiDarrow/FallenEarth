# HANDOFF — 2026-07-20_2356

## 1. Header
- **Date:** 2026-07-20 ~23:56 local
- **Session type:** OpenCode / grok-4.5
- **Focus:** Full-sphere hex packing + biome distribution; docs + push

## 2. What Was Accomplished
- Diagnosed uneven hex gaps + empty globe: axial disk + equirectangular/azimuthal patch cannot cover a sphere.
- Implemented **geodesic hexasphere** in `WorldGenerator` (icosahedron frequency-F, 12 pentagons + hexes).
- Full globe coverage (all 8 octants); tighter pack `HEX_PACK_RATIO=0.97`.
- Biome balance: climate envelopes + `BIOME_TARGET_WEIGHTS` + share rebalance (`MAX=0.18`, `MIN=0.04`). Medium Scorched ~10% (was ~35%).
- Updated 3D globe render (`WorldGeneration`) for graph-neighbor tangents; thin underlay sphere.
- `WorldMapScreen` lon/lat unwrap from `unit_pos`.
- Diag tools green: small/medium/large RESULT OK.
- Fixed runtime `Invalid cast … Vector3`: rebalance wrote JSON arrays into `_sphere_unit`; plus eager `dict.get(k, default_call())`.
- Updated `docs/ARCHITECTURE.md`, `memory/PROJECT_MEMORY.md`.

## 3. Key Decisions & Rationale
- **Full hexasphere over continent-patch** — user required whole sphere covered; disk projection cannot do that without singularity.
- **Keep `"q,r"` keys** (`q=id`, `r=0`) — minimizes breakage across GameState / missions / HubWorld; adjacency is `neighbor_keys`.
- **JSON-safe `unit_pos` as `[x,y,z]`** — Vector3 does not round-trip save cleanly.
- **Shared mesh size from min_nn × 0.97** — closer tiles without overlaps; nn_ratio ~1.24–1.37 acceptable for geodesic.
- See `docs/ARCHITECTURE.md` § Planet Hexasphere.

## 4. Modified Files
| Path | Change |
|------|--------|
| `scripts/WorldGenerator.gd` | Hexasphere build, climate/share, distance/neighbors, coerce helpers |
| `scripts/WorldGeneration.gd` | Globe render: graph tangent, safe positions, thinner base |
| `scripts/WorldMapScreen.gd` | 2D plot from unit_pos lon/lat |
| `tools/check_hex_spacing.py` | Python geodesic pack check |
| `tools/diag_hex_sphere_pack.gd` | Headless Godot gates (count/nn/pack/biomes/octants) |
| `tools/diag_hex_sphere_pack.gd.uid` | Godot uid |
| `docs/ARCHITECTURE.md` | Planet Hexasphere section |
| `memory/PROJECT_MEMORY.md` | 2026-07-20 entry |
| `memory/LATEST_HANDOFF.md` | Pointer update |
| `memory/SESSION_NOTES/HANDOFF_2026-07-20_2356.md` | This file |

## 5. Current State / Blockers
- **Working:** headless diag OK; user confirmed cast fix; full sphere + balanced biomes.
- **Not fully regression-tested in-game:** HubWorld edge travel, town placement, mission target hex, save/load of new worlds (old saves with axial disks are incompatible topology).
- **Cardinal local-map edges** still map to 4 directions; sphere tiles have 5–6 neighbors — strategic travel should use `neighbor_keys` / world map, not only cardinal edges.
- No blockers on packing/biome task itself.

## 6. Next Steps
1. F5 smoke: New Game → Medium FallenEarth → spin globe → pick start → HubWorld → M world map.
2. Verify `get_neighbors` / town spacing / mission pathing on graph keys.
3. Save → load world; confirm `load_from_tile_map` restores `_sphere_unit` / neighbors.
4. Optional: map HubWorld cardinal edges to best of 5–6 sphere neighbors by local facing.
5. Optional polish: per-tile hex scale near pentagons if gaps still visible at poles.

## 7. Context That Must Survive
- Tile count formula: `10*F²+2`. Size UI 8/12/18 → F 5/7/10.
- Do **not** use axial `[[±1,0],…]` neighbors on planet layer.
- Always `_coerce_vec3` / `unit_pos_vec` for positions.
- Never `positions.get(key, get_hex_spherical_pos(...))` — eager default eval.
- Diag: `Godot_v4.7.1-stable_win64_console.exe --headless --path . -s tools/diag_hex_sphere_pack.gd`
- Pack metrics target: `nn_ratio ≤ 1.40`, `pack_ratio ∈ [0.90, 0.99]`, biomes 8–14% share-ish.

## 8. Recommended Next Model/Provider
**Hermes / local** for F5 smoke + neighbor/travel wiring checks. Escalate to Claude Code only if HubWorld edge↔sphere neighbor mapping needs a larger design pass.

## 9. Relevant Memory/Skills
- `docs/ARCHITECTURE.md` — Planet Hexasphere
- `memory/PROJECT_MEMORY.md` — 2026-07-20 Full Hexasphere
- `memory/SESSION_NOTES/HANDOFF_2026-07-19_2145.md` — prior (mobs; unrelated)
- Skills used: load-project-context, prepare-handoff

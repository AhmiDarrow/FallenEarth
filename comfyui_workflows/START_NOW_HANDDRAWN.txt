=== START HERE - Hand-Drawn Asset Generation (Current Style) ===

1. Start ComfyUI if not running.

2. Generate Master Style (CRITICAL):
   Load: style_reference_generator.json
   Positive prompt: copy from handdrawn_master_prompt.txt
   Batch already set high (6). Queue 8-12 times.
   Pick best hand-drawn grim charming image.
   Save to: ComfyUI/input/master_style.png

3. Open: PROCEED_HANDDRAWN_GENERATION.txt for full plan.

4. First batch: Ash Wastes tiles
   Load: handdrawn_tileset_workflow.json
   Load master_style.png in IPAdapter node (0.85)
   Use prompts from: HANDDRAWN_TILES_FULL_BATCH.txt (Ash Wastes section)
   Generate, curate best.

Reply with progress (e.g. "master done" or "Ash Wastes 3/5 done") and next prompts/batches will be given.

Workflows ready with high batch (4-6).
Style is locked 2D hand-drawn (Stardew charm + gritty).

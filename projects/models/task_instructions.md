Inspect `model_scavenger.json` in the Fallen Earth Godot project and report strict JSON errors or structural drift.

Focus on: malformed JSON, mismatched values vs archetype definitions, inconsistencies with `character_archetypes.py`. Return exact line references and a concise fix map for every issue.